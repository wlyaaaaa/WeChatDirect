# WeChatDirect 项目约定

- 保留用户的六类入口：点名对象的有界聊天阅读、一个对象的首次及增量档案、AI 小上下文、正文和媒体同消息语境、当前朋友圈缓存读取或快照、明确点名时的按次保全。不要扩成全账号同步、常驻服务、画像、知识库或事件图。
- 源微信数据库与缓存只读；本机可见范围不是远端全历史。账号槽位隔离且以原生身份承诺校验，不从昵称、路径或另一个账号猜身份。
- 消息按原生时间、ID、发送者和引用关系交付。群聊本人归属不明时保留 unknown；`--self-only` 只覆盖本人及待定发送者，不据此断言全群从未参与。关键词页未命中须继续依据 coverage 和 continuation 判定。
- 原生媒体定位必须绑定同账号、同消息和可核验来源；`hardlink.db`、语音 `VoiceInfo`、原生 MD5 与解码结果各按现有路径处理。缺失原件不伪装成零内容或完整；图片/表情的含义由实际打开内容判断。
- `context` 仅读本机；显式 `export-context` 或 `media-open` 可按同消息的原生 CDN 地址物化表情并核对字节。`--local-only` 禁止该请求。保全不自动补抓远端表情。
- 阅读包和保全包的 `packageCreated=true` 只代表包已形成；`status=partial`、`delivery`、`coverage.hasMore` 和媒体缺口仍须单独解释。HTML 仅供人工查看，不是 AI 输入要求。
- 完成态档案增量前先验已有清单和文件；损坏保持原样。全量重核只针对点名对象，旧来源消失的记录保留但不冒充重新证明。定点修复不重扫历史、不推进游标。
- 事务和硬中断恢复按 `docs/RECOVERY.md` 执行；`verify-export` 只读，不能当成回微信的导入或缺失媒体修复。未知旧锁、未知半成品和被占用目录不自动删除。
- 配置、账号、聊天、媒体和真实导出不提交公开仓库。测试使用隔离配置及合成数据；主 CLI 需 Windows Python 3.14，语音解码是独立 Python 3.11 路线。用户命令见 `docs/USAGE.md`。
